# Get the variable str1 of type String as a user input (using the function input()). Create the
# list l1 each value of which is one character from the string str1 (example: if str1 = “lala”
# then l1 = [‘l’, ‘a’, ‘l’, ‘a’]). Print both str1 and l1.

str1 = input("Enter anything you want: ")
l1 = list(str1)

print("Converted list: ", l1)